import styled from "styled-components";

export const RegisterPageStyle = styled.div`
  h1 {
    margin: 1rem 0;
  }

  .criar-conta {
    margin-top: 0.6rem;
  }

  p.redirect {
    margin-top: 1.2rem;
  }
`;
